# `single()`：单个方块坐标

仅表示一个单一的方块坐标的[区域](../zh.md)。

## 语法

- `<坐标>`
- `single(<坐标>)`

## 参数

### `<坐标>`

单一的[方块坐标](../../pos/zh.md)。

## 示例

- `single(~~~)`
- `single(1 2 3)`
- `single(^^^5)`

## 数据结构

- **type**：字符串。当前为 `enhanced_commands:single`。
- **pos**：数组。
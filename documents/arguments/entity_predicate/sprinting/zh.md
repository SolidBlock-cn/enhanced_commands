# `sprinting`：检测疾跑

此[实体谓词](../zh.md)测试实体是否存在疾跑。此参数可以取反。

## 示例

- `@e[sprinting=true]`：选择正在疾跑的实体。
- `@a[sprinting=false]`：选择未在疾跑的实体。

## 数据结构

- `type`：此处为 `"enhanced_commands:sprinting"`。
- `expected`：布尔值。
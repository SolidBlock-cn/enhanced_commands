# `collector`

此[实体谓词](../zh.md)是技术性的存在。在输入实体选择器时，可以指定一些[更多的实体选择器类型](../../entity_selector/zh.md#更多的实体选择器类型)，这些类型会按照各自的方式收集实体，同时也可用作实体谓词。

此实体谓词不支持通过实体选择器的选项指定，而是通过实体选择器的类型指定的。

## 数据结构

- `type`：此时为 `"enhanced_commands:collector"`。
- `collector`：字符串。
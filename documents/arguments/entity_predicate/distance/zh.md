# `distance`

此[实体谓词](../zh.md)是技术性的存在，用于检测实体到实体选择器的 `x`、`y`、`z` 参数指定的位置的距离。

在实体选择器参数中，距离的取值范围由 `distance` 参数指定，距离的基准点由 `x`、`y`、`z` 参数指定。

## 数据结构

- `type`：此时为 `"enhanced_commands:distance"`。
- `distance`：双精度浮点数或映射。
    - `min`：双精度浮点数。
    - `max`：双精度浮点数。
- `info`：映射。表示由 `x`、`y`、`z` 参数指定的坐标偏移方法。
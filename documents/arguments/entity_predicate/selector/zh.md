# `selector`：基于实体选择器的谓词

此[实体谓词](../zh.md)检测实体是否符合指定的实体选择器。这是模组中的实体谓词最主要的实现方式。

当实体选择器指定了 `limit` 参数时，作为实体谓词使用时会先选择实体，再判断实体是否属于被选择的实体。如果没有指定 `limit` 参数，则直接使用该选择器所指定的实体谓词进行判断。

## 数据结构

- `type`：此处为 `"enhanced_commands:selector"`。
- 其他字段与[实体选择器](../../entity_selector/zh.md)的字段一致。
# `pitch`: Tests entity's yaw angle

This [entity predicate](../en.md) tests entity's yaw angle. Same as vanilla entity selector's `y_rotation` argument.

## Data structure

- `type`: Currently `"enhanced_commands:yaw"`.
- `min`: Floating-point number.
- `max`: Floating-point number.
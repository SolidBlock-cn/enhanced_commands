# `player_only`: Predicate that filters players only

This [entity predicate](../en.md) passes only when the entity is a player. This predicate is technical, and cannot be directly defined by entity selector arguments.

## Data structure

- `type`: Currently `"enhanced_commands:player_only"`.

No other fields.
# `nbt`

This [entity predicate](../en.md) tests entity's NBT. Same as vanilla entity selector's `nbt` argument.

## Data structure

- `type`: Currently `"enhanced_commands:nbt"`.
- `nbt`: Map.
- `inverted`: Boolean, optional, by default `false`.
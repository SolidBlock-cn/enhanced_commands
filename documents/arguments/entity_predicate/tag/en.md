# `tag`: Tests scoreboard tags

This [entity predicate](../en.md) tests the player's scoreboard tag, identical to vanilla entity selector's `tag` argument.

## Data structure

- `type`: Currently `"enhanced_commands:tag"`.
- `tag`: String. Required, but can be an empty string.
- `inverted`: Boolean, optional, by default `false`.
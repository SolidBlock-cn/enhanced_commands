# `box`

此[实体谓词](../zh.md)用于限制查找实体的范围，此实体谓词仅是技术性的存在，因为原版的一些实体选择器选项（如 `distance`、`dx`、`dy`、`dz`）会限制实体查找的范围。

此实体谓词不支持通过实体选择器参数指定，请使用 [`region` 选项](../region/zh.md)。

## 数据结构

- `type`：此时为 `"enhanced_commands:box`。
- 其余所有字段均不支持。
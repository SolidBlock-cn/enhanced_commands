# `alive`

此[实体谓词](../zh.md)检测实体是否存活。目前暂不支持通过实体选择器参数指定，可通过不同的实体选择器类型指定。

## 示例

- `@e`：所有存活的实体。
- `@a`：所有玩家，包括死亡的。
- `@e[type=player]`：所有存活的玩家。
- `@E`：所有实体，包括死亡的。
- `@a[not=@e]`：死亡的玩家。
- `@E[not=@e]`：死亡的实体。

## 数据结构

无额外字段。

- `type`：此时为 `"enhanced_commands:alive"`。
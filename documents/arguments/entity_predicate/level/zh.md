# `level`

此[实体谓词](../zh.md)用于测试玩家的经验等级，用法和原版基本相同，区别在于本模组支持将其取反。

使用了此谓词的实体选择器将只能选择玩家。

## 示例

- `@a[level=10..]`：选择经验等级至少为 10 的玩家。
- `@a[level=!5]`：选择经验等级不为 5 的玩家。

## 数据结构

- `type`：此处为 `"enhanced_commands:level"`。
- `level`：整数取值范围。
- `inverted`：布尔值，可选，默认为 `false`。
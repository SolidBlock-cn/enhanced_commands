# `pitch`: Tests entity's pitch angle

This [entity predicate](../en.md) tests entity's pitch angle. Same as vanilla entity selector's `x_rotation` argument.

## Data structure

- `type`: Currently `"enhanced_commands:pitch"`.
- `min`: Floating-point number.
- `max`: Floating-point number.
# `scores`：测试实体的记分板分数

此[实体谓词](../zh.md)测试实体的记分板分数，与原版的 `scores` 参数类似，接收一个“记分项名称-取值范围”的映射（键值对），并且此模组支持将取值范围取反。

当指定的记分项不存在，或者实体在该记分项上没有分数时，测试一定失败，无论是否取反。

## 语法

- `[scores={<记分项名称> = <范围>}]`：选择在该记分项上的分数在指定的范围内的实体。
- `[scores={<记分项名称> =! <范围>}]`：选择在该记分项上有分数，且分数不在指定的范围内的实体。

## 示例

这里只展示部分非原版例子。

- `@a[scores={a=!2}]`：选择在记分项 a 上有分数，且分数 a 的值不为 2 的玩家。
- `@e[scores={a=!3, b=!5..6}]`：选择记分项 a 和 b 上均有分数，且分数 a 的值不为 3、分数 b 的值不在 5 到 6 范围内的存活实体。

## 数据结构

- `type`：`enhanced_commands:scores`。
- `scores`：列表。
    - 列表中的元素。映射。
        - `name`：字符串。
        - `score`：整数范围。
        - `inverted`：布尔值，可选，默认为 `false`。
# `pos()` 函数：在 NBT 中表示坐标

此 [NBT 函数](../zh.md) 用于以数组的形式返回坐标。虽然也可以直接写数组，但是此 NBT 函数支持各种复杂的坐标语法，包括相对坐标和局部坐标。

## 语法

- `pos(<位置>)`

## 参数

### `<位置>`

[坐标](../../pos/zh.md)。支持相对坐标和局部坐标。

## 示例

- `pos(1 5 4)`
- `pos(~ ~ ~3)`
- `pos(^ ^ ^2)`